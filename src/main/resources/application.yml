spring:
  profiles:
    active: default
    
server:
  port: 8102    

---
spring:
  profiles: default
  
  datasource:
    url: jdbc:postgresql://localhost:5432/company_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver